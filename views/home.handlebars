<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    <h1>{{title}}</h1>
    <ul>
        {{#each products}}
        <li>
            {{this.title}} - ${{this.price}}
        </li>
        {{/each}}
    </ul>

    <nav>
        {{#if hasPrevPage}}
        <a href="/home?page={{prevPage}}&limit={{limit}}">Anterior</a>
        {{/if}}
        Página {{currentPage}} de {{totalPages}}
        {{#if hasNextPage}}
        <a href="/home?page={{nextPage}}&limit={{limit}}">Siguiente</a>
        {{/if}}
    </nav>
</body>
</html>
